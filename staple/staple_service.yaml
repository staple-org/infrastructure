kind: Service
apiVersion: v1
metadata:
  namespace: staple
  name: staple
  labels:
    app: staple
    app.kubernetes.io/name: staple
    app.kubernetes.io/instance: staple
spec:
  selector:
    app: staple
    app.kubernetes.io/name: staple
    app.kubernetes.io/instance: staple
  ports:
  - name: staple-service
    port: 9998
    targetPort: staple-port
    protocol: TCP