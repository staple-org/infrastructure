kind: Service
apiVersion: v1
metadata:
  namespace: staple
  name: staple
  labels:
    app: staple
    app.kubernetes.io/name: staple
    app.kubernetes.io/instance: staple
spec:
  selector:
    app: staple
    app.kubernetes.io/name: staple
    app.kubernetes.io/instance: staple
  ports:
  - port: 80
    targetPort: 9998