apiVersion: v1
kind: Secret
metadata:
  name: staple-db-password
  namespace: staple
data:
  POSTGRES_PASSWORD: asdf
  # This creates a user and a db with the same name. This will be staples.
  POSTGRES_USER: asdf==
